import 'package:flutter/material.dart';

class DetailModulSiswaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Modul'),
      ),
      body: Center(
        child: Text('Ini adalah halaman detail modul siswa'),
      ),
    );
  }
}
